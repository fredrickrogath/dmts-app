import 'package:flutter/material.dart';
class KidneyAbility extends StatelessWidget {
  const KidneyAbility({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'Kidney ablility'
    );
  }
}